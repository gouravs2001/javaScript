/**
 *                         body
 *                          ^
 *                          |
 * Window -> document -> html ---> head
 *
 * innerText property show only the visible part.
 * innerContent property shows everything inside tag wheather it is set to display:none(not visible) or not(visible).
 * innerHTML - gives all the HTML attributes present in the tag.
 *
 * document.getElementByClassName("list-item") returns HTML-collections
 * document.querySelectorAll("h1") returns NodeList
 * we can convert HTMLcollections or NodeList to array by Array.from() method.
 *
 */
//REREAD
