// input taken during function defination => parameter e.g. name in this function

function sayMyName(name = " default ") {
  console.log(name);
}

// data passed during function call => Arguments e.g. Heisenberg is argument here

sayMyName("Heisenberg");
